# Contribution-specific extension merge

Extension Union does not apply one global duplicate-key rule. Each Extension Contribution type defines its own Contribution Merge semantics, allowing schema specs, attributes, commands, keymaps, plugins, and service requirements to compose according to their own editor-domain rules.
